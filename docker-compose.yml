services:
  dashboard:
    image: gurururu/logtech-dashboard:latest
    container_name: dashboard
    restart: unless-stopped
    ports:
      - "80:3000"
    environment:
      NODE_ENV: "production"
      OBSERVABILITY_SERVICE_URL: "http://localhost:8081"
